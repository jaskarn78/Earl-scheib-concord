# Claude Code Guidelines

## Before Making Changes

Always pull the latest from `main` before starting any work:

```bash
git pull origin main
```

This prevents conflicts and ensures edits are based on the current state of the repo.
